# Ashmit Sharma — Portfolio

A modern, new-grad ready portfolio built with React, Vite, and React Router.

## Run locally

```bash
npm install
npm run dev
```

Open [http://localhost:5173](http://localhost:5173).

## Build for production

```bash
npm run build
```

Output goes to the `dist/` folder. For SPA routing, configure your host to serve `index.html` for all routes (e.g. Vercel, Netlify, GitHub Pages SPA fallback).
